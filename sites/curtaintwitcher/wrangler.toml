name = "atprotozoa-curtaintwitcher"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# @fromthewestmeadow.com: "Make a fake Nextdoor-style social app where users
# post increasingly paranoid neighborhood updates, reply in character, and
# vote on whether each complaint is real or completely unhinged."
#
# The feed is browser-owned: a hand-written seed plus local posts and votes.
# The meter is only this browser's view, not a shared town-wide authority.
#
# routes MUST stay above [assets] — in TOML, keys after a table header join
# that table, so a routes line below [assets] silently becomes assets.routes.
routes = [{ pattern = "curtaintwitcher.bisks.net/*", zone_name = "bisks.net" }]

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true

# Historical migration retained so deployed Board instances can be cleaned up.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Board"]

[[migrations]]
tag = "v2"
deleted_classes = ["Board"]
