name = "atprotozoa-blockledger"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# RETIRED: this site now serves only a static retirement stub (see
# RETIRED.md and src/index.ts). The Worker just serves public/.
routes = [{ pattern = "blockledger.bisks.net/*", zone_name = "bisks.net" }]

[assets]
directory = "./public"
binding = "ASSETS"
