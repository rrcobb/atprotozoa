name = "atprotozoa-catspace"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# catspace (catspace.bisks.net): MySpace for cats. @me.jamespicone.name asked
# for it in a reply, @bisks.net gave the go-ahead. Each cat's profile is a
# net.bisks.catspace.profile record ("self") in its owner's own PDS — this
# Worker never stores the profile content itself. The REGISTRY Durable Object
# is a shared index over profiles that individual PDSes can't be queried for:
# the directory listing, visitor counters, and the guestbook (comments live in
# each *commenter's* own PDS, so displaying them on someone else's profile
# needs somewhere to aggregate).
#
# New site, single canonical host — no legacy bisks.net/<name> path route.
routes = [
  { pattern = "catspace.bisks.net/*", zone_name = "bisks.net" },
]

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true

[[durable_objects.bindings]]
name = "REGISTRY"
class_name = "Registry"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["Registry"]

[observability]
enabled = true
