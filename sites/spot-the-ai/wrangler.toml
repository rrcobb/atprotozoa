name = "atprotozoa-spot-the-ai"
compatibility_date = "2025-01-01"

# Pure static site: a 10-round "spot the AI illustration" quiz. No server
# surface needed — everything runs client-side against public/images/.
assets = { directory = "./public" }

routes = [{ pattern = "spot-the-ai.bisks.net", custom_domain = true }]
