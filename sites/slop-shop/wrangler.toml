name = "atprotozoa-slop-shop"
compatibility_date = "2025-01-01"

# Fully static: the flyer generator, ad-copy generator, and Bluesky OAuth
# posting flow all run client-side in public/. No server surface needed.
assets = { directory = "./public" }

routes = [{ pattern = "slop-shop.bisks.net", custom_domain = true }]
