# 2026-08-15: redeploy trigger, edit never actually shipped — caught in the
# 1001nights pnpm-lockfile CI outage, see notes/history/2026-08-pnpm-lockfile-outage.md.
name = "atprotozoa-likescore"
compatibility_date = "2025-01-01"

routes = [
  { pattern = "likescore.bisks.net/*", zone_name = "bisks.net" },
]

[assets]
directory = "./public"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["LikeScoreEngine"]

[[migrations]]
tag = "v2"
deleted_classes = ["LikeScoreEngine"]
