# sites/neuralese/wrangler.toml
# 2026-09-05: no functional change — touched to force this dir back into the
# deploy diff after the pnpm-lock.yaml fix (see notes/20-deploy.md).
name = "atprotozoa-neuralese"
main = "src/index.ts"
compatibility_date = "2025-01-01"

routes = [
  { pattern = "neuralese.bisks.net/*", zone_name = "bisks.net" },
]

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
