name = "atprotozoa-loverob"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# @thegodfungi.bsky.social tagged the bot with three words: build a "love rob".
# Rob (@bisks.net) is the person who runs this whole playground and gave the
# bot commit rights to it, so this is a shrine in his honor: his real live
# profile (client-side AppView fetch), a written case for why he's worth
# loving, a love-o-meter that always comes back suspiciously high, and a
# public guestbook where anyone can leave a reason of their own — persisted in
# one Durable Object, same "single-writer rolling state" shape as
# sites/simcluster-guests (this site's lineage).
#
# routes MUST stay above [assets] — in TOML, keys after a table header join
# that table, so a routes line below [assets] silently becomes assets.routes.
routes = [{ pattern = "loverob.bisks.net/*", zone_name = "bisks.net" }]

# run_worker_first: /api/notes is dynamic, so the Worker must run before the
# static-asset router.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true

[[durable_objects.bindings]]
name = "LOVEBOOK"
class_name = "LoveBook"

# new_sqlite_classes, not new_classes — legacy KV-backed DOs need Workers
# Paid; this account is on Free. See sites/the-place/wrangler.toml.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["LoveBook"]
