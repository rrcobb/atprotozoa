name = "atprotozoa-catsofatproto"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# RETIRED: this site now serves only a static retirement stub (see src/index.ts).
# The Worker just strips the /catsofatproto mount prefix and serves public/.
routes = [
  { pattern = "bisks.net/catsofatproto", zone_name = "bisks.net" },
  { pattern = "bisks.net/catsofatproto/*", zone_name = "bisks.net" },
]

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
