{
  "lexicon": 1,
  "id": "net.bisks.void.vote",
  "defs": {
    "main": {
      "type": "record",
      "description": "One voter's vote on a shout/echo/murmur. rkey is deterministic (derived from the voter's own DID + subjectUri), so casting a second vote on the same subject overwrites the first via putRecord rather than creating a duplicate — one vote per voter per subject, idempotent by construction.",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["subjectUri", "value", "createdAt"],
        "properties": {
          "subjectUri": { "type": "string", "format": "at-uri" },
          "value": {
            "type": "integer",
            "enum": [1, -1],
            "description": "1 = up, -1 = down."
          },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
