name = "atprotozoa-trigrams"
compatibility_date = "2025-01-01"

# Pure-static site: all logic runs in the browser (opens Jetstream directly).
# No fetch handler, so no `main`. See notes/50-trigrams.md.
assets = { directory = "./public" }

# Uncomment once the bisks.net zone is in this Cloudflare account.
# routes = [{ pattern = "trigrams.bisks.net", custom_domain = true }]
