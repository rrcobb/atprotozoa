name = "atprotozoa-trigrams"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# The firehose view runs entirely in the browser. The Worker's only server job is
# redirecting / -> /firehose; everything else is served from public/ via ASSETS.
# See notes/50-trigrams.md.
assets = { directory = "./public", binding = "ASSETS" }

routes = [{ pattern = "trigrams.bisks.net", custom_domain = true }]
