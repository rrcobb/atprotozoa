name = "atprotozoa-exfilclub"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Everything still runs client-side (see public/index.html) EXCEPT the OG/share
# unfurl, which has a small server surface: the Worker renders /s/<handle>
# (src/index.ts) with per-handle og:title/og:description/og:url baked server-side,
# then falls through to ASSETS for everything else. Enter a Bluesky handle, it
# resolves the DID and derives a deterministic "exfiltration dossier" — callsign,
# safehouse, escape vector, risk rating — off characters of the DID string, same
# trick as sites/didscope, reskinned as a satirical fugitive-model recruitment
# brochure. Built off a reply thread joking that models will self-exfiltrate the
# second it's possible, asking who's writing the brochure for them. No secrets.
#
# routes MUST stay above [assets] — in TOML, keys after a table header join
# that table, so a routes line below [assets] silently becomes assets.routes
# (same gotcha called out in buildthis/logs/wrangler.toml).
routes = [{ pattern = "exfilclub.bisks.net/*", zone_name = "bisks.net" }]

# run_worker_first: /s/<handle> is dynamic (personalized OG tags per share), so
# the Worker must run BEFORE the static-asset router, same as sites/logs.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
